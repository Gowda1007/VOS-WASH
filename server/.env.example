# MongoDB Connection
MONGODB_URI=mongodb+srv://username:password@cluster.mongodb.net/vosWashDB?retryWrites=true&w=majority

# Server Configuration
PORT=3001

# Security
API_KEY=your_secret_api_key_here

# CORS Configuration (optional)
# Comma-separated list of additional allowed origins
ALLOWED_ORIGINS=http://192.168.1.100:3000,http://localhost:5173

# Set to 'true' to allow all origins (testing only, not for production)
ALLOW_ALL_CORS=false

# Client URL (used for CORS if needed)
VITE_SERVER_URL=http://localhost:3001
