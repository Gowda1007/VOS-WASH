import { AppRegistry } from 'react-native';
import App from './App';

// For a React Native app, you typically register your main component here.
// The name 'VOSWASH' should match the name in your app.json file (if you create one).
AppRegistry.registerComponent('VOSWASH', () => App);