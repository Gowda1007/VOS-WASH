export const invoiceLayout = {
  // Base sizes used in RN preview
  logoSize: 36, // px
  variWidthFactor: 1.8, // Sri-Vari width relative to logo size

  // Preview container spacing
  previewMargin: 16, // fallback if theme spacing not available
};

export const pdfScale = 1.6; // scale RN sizes to PDF points for readability
