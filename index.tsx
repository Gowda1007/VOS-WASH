import { AppRegistry } from 'react-native';
import App from './App';

// Register the main component of your React Native app.
// The name 'VOSWASH' should match the name in your app.json.
AppRegistry.registerComponent('VOSWASH', () => App);